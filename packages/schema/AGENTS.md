# packages/schema — AGENTS.md

JSON Schema is source of truth under `schemas/`. Run `pnpm schema:build` after changes.

Emit Zod to `src/generated/`. Python Pydantic mirrors live in `apps/api` until full codegen lands.

Breaking changes require `docs/adr/`.
